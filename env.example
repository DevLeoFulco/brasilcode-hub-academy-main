# BrasilCodeGap - Variáveis de Ambiente
# Copie este arquivo para .env e ajuste conforme necessário

# Configuracao para o Front End
REACT_APP_API_URL=http://localhost:3000/api

# Configuração para o PostgreSQL 
POSTGRES_DB=brasilcodegap
POSTGRES_USER=leofulco
POSTGRES_PASSWORD=admin2501
POSTGRES_INITDB_ARGS=--encoding=UTF-8 --lc-collate=C --lc-ctype=C

# Configuracao do pgAdmin 
PGADMIN_DEFAULT_EMAIL=admin@brasilcodegap.com
PGADMIN_DEFAULT_PASSWORD=admin2501
PGADMIN_CONFIG_SERVER_MODE=False

# Configuracao do Docker
COMPOSE_PROJECT_NAME=brasilcodegap 